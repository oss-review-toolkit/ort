# Configuration of Detekt rule sets, see: https://github.com/arturbosch/detekt#rulesets

build:
    failThreshold: 26

complexity:
    LargeClass:
        threshold: 150
    NestedBlockDepth:
        threshold: 4
