// Apply core plugins.
apply plugin: 'application'

applicationName = 'downloader'
mainClassName = 'com.here.provenanceanalyzer.downloader.Main'

dependencies {
    compile project(':model')
    compile project(':util')

    compile "com.beust:jcommander:$jcommanderVersion"
}
