# Flox manifest version managed by Flox CLI
version = 1

[vars]
FLOX_DISABLE_METRICS = "true"

[options]
activate.mode = "run"
cuda-detection = false
systems = ["x86_64-linux"]

[install]
licensee.pkg-path = "licensee"
licensee.version = "9.18.0"
