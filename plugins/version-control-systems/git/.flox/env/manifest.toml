schema-version = "1.10.0"

[vars]
FLOX_DISABLE_METRICS = "true"

[options]
activate.mode = "run"
cuda-detection = false
systems = ["x86_64-linux"]

[install]
git.pkg-path = "git"
git-repo.pkg-path = "git-repo"
